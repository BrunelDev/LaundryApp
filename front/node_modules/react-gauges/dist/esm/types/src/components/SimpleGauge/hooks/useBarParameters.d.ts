import { type SimpleGaugeParameters } from "../../../core/simpleGauge/simpleGauge.types";
export declare function useBarParameters(width: number, barWidth: number, value: number, minLimit: number, maxLimit: number, isTotal: boolean): {
    circleParameters: SimpleGaugeParameters;
    baseCircleParameters: SimpleGaugeParameters;
    width: number;
};
