export declare const getValidValue: (value: number, limits: {
    min: number;
    max: number;
}) => number;
export declare const getLabel: (value: number, template: string, limits: {
    min: number;
    max: number;
}) => string;
